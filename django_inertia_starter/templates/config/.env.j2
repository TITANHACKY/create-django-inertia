# Local environment for {{ project_name }}. Not committed to git.
SECRET_KEY={{ secret_key }}
DEBUG=True

# Comma-separated. Leave empty in development (Django allows localhost when DEBUG=True).
ALLOWED_HOSTS=
